@layout.app() 
    @slot('main')
    <div class="min-h-screen bg-gradient-to-br from-green-50 to-green-100 flex items-center justify-center px-4 py-8">
        <div class="w-full max-w-md">
            <div class="bg-white shadow-2xl rounded-xl overflow-hidden">
                <div class="p-8">
                    <h2 class="text-3xl font-bold text-center text-green-800 mb-6">Login</h2>
                    
                    <form action="{{ route('login') }}" method="POST" class="space-y-6">
                        {{ csrfField() }}
                        
                        {{-- @if(flashMessage('error'))
                            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
                                {{ flashMessage('error') }}
                            </div>
                        @end --}}
    
                        <div>
                            <label for="email" class="block text-sm font-medium text-green-700 mb-2">Email Address</label>
                            <input 
                                type="email" 
                                id="email" 
                                name="email" 
                                required 
                                value="{{ old('email', '') }}"
                                placeholder="Enter your email" 
                                class="w-full px-4 py-2 border-2 border-green-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500 transition duration-300 @if(flashMessage('errors.email')) border-red-500 @end"
                            >
                            {{-- @if(flashMessage('errors.email'))
                                <p class="text-red-500 text-xs mt-1">{{ flashMessage('errors.email') }}</p>
                            @end --}}
                        </div>
                        
                        <div>
                            <label for="password" class="block text-sm font-medium text-green-700 mb-2">Password</label>
                            <input 
                                type="password" 
                                id="password" 
                                name="password" 
                                required 
                                placeholder="Enter your password" 
                                class="w-full px-4 py-2 border-2 border-green-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500 transition duration-300 @if(flashMessage('errors.password')) border-red-500 @end"
                            >
                            {{-- @if(flashMessage('errors.password'))
                                <p class="text-red-500 text-xs mt-1">{{ flashMessage('errors.password') }}</p>
                            @end --}}
                        </div>
                        
                        <div class="flex items-center justify-between">
                            <div class="flex items-center">
                                <input 
                                    id="remember" 
                                    name="remember" 
                                    type="checkbox" 
                                    class="h-4 w-4 text-green-600 focus:ring-green-500 border-gray-300 rounded"
                                >
                                <label for="remember" class="ml-2 block text-sm text-green-900">
                                    Remember me
                                </label>
                            </div>
                            
                            {{-- <div class="text-sm">
                                <a href="{{ route('password.request') }}" class="font-medium text-green-600 hover:text-green-500">
                                    Forgot password?
                                </a>
                            </div> --}}
                        </div>
                        
                        <div>
                            <button 
                                type="submit" 
                                class="w-full flex justify-center py-3 px-4 border border-transparent rounded-full shadow-sm text-sm font-medium text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 transition duration-300 ease-in-out transform hover:scale-105"
                            >
                                Sign in
                            </button>
                        </div>
                    </form>
                </div>
                
                <div class="px-8 py-4 bg-green-50 text-center">
                    <p class="text-sm text-green-600">
                        Don't have an account? 
                        {{-- <a href="{{ route('register') }}" class="font-medium text-green-800 hover:underline">
                            Register here
                        </a> --}}
                    </p>
                </div>
            </div>
        </div>
    </div>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const form = document.querySelector('form');
            const emailInput = document.getElementById('email');
            const passwordInput = document.getElementById('password');
    
            form.addEventListener('submit', function(e) {
                let isValid = true;
    
                // Email validation
                if (!emailInput.value.trim()) {
                    isValid = false;
                    emailInput.classList.add('border-red-500');
                    // You could add error message display logic here
                } else {
                    emailInput.classList.remove('border-red-500');
                }
    
                // Password validation
                if (!passwordInput.value.trim()) {
                    isValid = false;
                    passwordInput.classList.add('border-red-500');
                    // You could add error message display logic here
                } else {
                    passwordInput.classList.remove('border-red-500');
                }
    
                if (!isValid) {
                    e.preventDefault();
                }
            });
        });
    </script>
    @end
    
@end

